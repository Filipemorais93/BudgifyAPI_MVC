using BudgifyAPI.Wallet.Models.DB;

namespace BudgifyAPI.Wallet.Services
{
    public interface IWalletService
    {
        //Wallet
        //Task<bool> AddWallet(Wallet wallet);
        //Task<bool> UpdateWallet(Guid walletId);
        //Task<bool> DeleteWallet(Guid walletId);
        //Task<bool> GetWallet(Wallet wallet);

        //Faltam mais 2
    }
}
